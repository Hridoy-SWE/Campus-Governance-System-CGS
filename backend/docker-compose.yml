//1175
version: "3.9"

services:
  backend:
    build: .
    container_name: campus-governance-backend
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ../database:/app/database
      - ./uploads:/app/uploads
    restart: unless-stopped
